import { Container } from "@mui/material";

export const CenterPageContainer = () => {
    return(
        <Container maxWidth={'md'}/>
    )
}

export const WidePageContainer = () => {
    return(
        <Container maxWidth={'lg'}/>
    )
}